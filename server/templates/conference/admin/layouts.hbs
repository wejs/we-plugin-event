<div class="row">
  <div class="col-lg-12">
    <ul class="nav nav-pills">
      {{#each conferenceLayouts}}
        <li role="presentation">
          {{#link-to 'conference_admin_layout' ../conference.id @key}}{{@key}}{{/link-to}}
        </li>
      {{/each}}
    </ul>
  </div>
</div>