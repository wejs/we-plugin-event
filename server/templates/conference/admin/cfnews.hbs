<div class="row">
  <div class="col-lg-12 actions">{{we-action-create-btn model req conference.id}}</div>
  <div class="col-lg-12">
    <table class="table table-striped table-hover">
      <thead>
        <tr>
          <th>id</th>
          <th>{{t 'title'}}</th>
          <th>{{t 'teaser'}}</th>
          <th>{{t 'actions'}}</th>
        </tr>
      </thead>
      <tbody>
      {{#each record}}
        <tr>
          <td>{{id}}</td>
          <td>{{title}}</td>
          <td>{{teaser}}</td>
          <td>
            {{we-action-view-btn 'cfnews' this ../req conferenceId id }}
            {{we-action-edit-btn 'cfnews' this ../req conferenceId id }}
            {{we-action-delete-btn 'cfnews' this ../req conferenceId id }}
          </td>
        </tr>
      {{/each}}
      </tbody>
      <tfoot>
        <tr>
          <td colspan="4">
            <div class="paginate-sumary">
              {{t 'paginate.sumary' recordsLength=record.length count=metadata.count}}
            </div>
            {{paginate count=metadata.count limit=query.limit currentPage=query.page req=req}}
          </td>
        </tr>
      </tfoot>
    </table>

  </div>
</div>