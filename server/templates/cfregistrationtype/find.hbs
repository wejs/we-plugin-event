<div class="row">
  <div class="col-lg-12">
    {{we-action-create-btn 'cfregistrationtype' req conference.id }}
  </div>
  <div class="col-lg-12">
    <table class="table table-striped table-hover">
      <thead>
        <tr>
          <th>id</th>
          <th>{{t 'cf.registration.name'}}</th>
          <th>{{t 'requireValidation'}}</th>
          <th>{{t 'actions'}}</th>
        </tr>
      </thead>
      <tbody>
      {{#each record}}
        <tr>
          <td>{{id}}</td>
          <td>{{name}}</td>
          <td>{{requireValidation}}</td>
          <td>
            {{we-action-view-btn 'cfregistrationtype' this ../req conferenceId id }}
            {{we-action-edit-btn 'cfregistrationtype' this ../req conferenceId id }}
            {{we-action-delete-btn 'cfregistrationtype' this ../req conferenceId id }}
          </td>
        </tr>
      {{/each}}
      </tbody>
    </table>
  </div>
</div>