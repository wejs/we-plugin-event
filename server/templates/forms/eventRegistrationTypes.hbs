<we-event-registration-types-selector
  data-event-id="{{locals.event.id}}"
  data-label-name="{{t 'form-cfregistrationtype-name'}}"
  data-label-price="{{t 'form-cfregistrationtype-price'}}"
  data-label-period="{{t 'form-cfregistrationtype-period'}}"
  data-label-description="{{t 'form-cfregistrationtype-description'}}"
  data-label-actions="{{t 'actions'}}"
  data-text-loading="Loading ..."
  data-label-create="{{t 'add'}}"
  data-label-save="{{t 'save'}}"
  data-label-remove="{{t 'remove'}}"
  data-message-remove="{{t 'cfregistrationtype.delete.confirm.msg'}}"
  data-form-id="{{formId}}"
  data-dateformat="{{locals.req.we.config.date.defaultFormat}}"
  data-max-date="{{locals.event.eventEndDate}}"
></we-event-registration-types-selector>